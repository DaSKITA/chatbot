# This is an example SSL configuration
# Uncomment and adapt for your deployment as needed
listen                  8443 ssl;

server_name           implementation.cloud;
ssl_certificate         /etc/certs/fullchain.pem;
ssl_certificate_key     /etc/certs/privkey.pem;
